services:
  server:
    env_file: .env
    build:
      context: .
      args:
        - REACT_APP_API_URL=$API_URL
    container_name: bia
    ports:
      - "80:8080"
    environment:
      - REACT_APP_API_URL=$API_URL
     